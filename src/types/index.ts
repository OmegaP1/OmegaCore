export type ServiceType = {
  id: string;
  title: string;
  description: string;
  icon: string;
};
  
export type NavItem = {
  label: string;
  href: string;
};

export type SocialLink = {
  platform: string;
  url: string;
  icon: string;
};